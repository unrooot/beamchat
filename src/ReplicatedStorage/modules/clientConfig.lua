local config = {
	-- The amount of messages that will show up before they get cleared.
	chatLimit = 300;

	-- The chat animation type. "modern" for a tween/fade, classic for just a fade.
	chatAnimation = "modern";
}

return config